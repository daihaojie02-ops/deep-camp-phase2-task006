# 需求规格 — spec.md

## 用户

- **主要用户：** 戴昊杰（开发者，日常在终端工作）
- **使用频率：** 每天 1-3 次，每次一行
- **技术能力：** 会用命令行，不需要 GUI

## 输入

- **添加日志：** `daily add "一句话内容"` — 任意文本字符串
- **查看最近：** `daily list` 或 `daily list -n N` — 可选数字参数
- **查看今日：** `daily today` — 无参数
- **搜索：** `daily search "关键词"` — 任意文本字符串

## 输出

- **添加后：** 终端显示确认消息 + 日志文件路径
- **列出时：** 终端逐行显示日志记录，今天高亮，包含总数统计
- **搜索时：** 终端显示匹配条目
- **持久化：** 所有日志追加到 `~/.daily_log.md` 文件

## 约束

1. **不依赖数据库** — 纯文本 Markdown 文件，任何编辑器能打开
2. **不依赖网络** — 完全本地运行
3. **不依赖第三方库** — Python 标准库即可
4. **单文件工具** — 一个 `daily.py` 文件
5. **日志格式固定** — `- [YYYY-MM-DD HH:MM] 内容`

## 明确不做的范围

- ❌ 不做网页界面
- ❌ 不做手机端
- ❌ 不做日历视图
- ❌ 不做数据库存储
- ❌ 不做标签/分类系统（需求方没说）
- ❌ 不做统计/图表（需求方没说）
- ❌ 不做多用户/多端同步（需求方没提）
- ❌ 不做 Eval/回归测试/盲测（这是 004 的边界，006 只做到最小可运行）
