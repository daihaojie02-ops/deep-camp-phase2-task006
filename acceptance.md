# 验收标准 — acceptance.md

## 验收条件（≥5 条，每条可 yes/no 判定）

| # | 验收条件 | 判定方式 |
|---|---------|---------|
| **AC1** | `daily add "内容"` 执行后，日志文件 `~/.daily_log.md` 中新增一行包含该内容和当前时间戳 | 打开文件检查是否有新行 |
| **AC2** | `daily list` 执行后，终端显示最近 10 条日志（如果不足 10 条则显示全部） | 终端输出对比文件内容 |
| **AC3** | `daily today` 执行后，终端只显示今天日期的日志，不显示历史 | 终端输出只含当天日期 |
| **AC4** | `daily search "关键词"` 执行后，终端只显示包含该关键词的日志条目 | 搜索一个已知关键词，验证结果 |
| **AC5** | 日志文件是标准 Markdown 格式，用任意文本编辑器可以直接打开阅读 | 用 notepad/VSCode 打开 `~/.daily_log.md` |
| **AC6** | 首次运行时自动创建日志文件，不需要手动初始化 | 删除文件后运行 `daily list`，文件应自动生成 |
| **AC7** | `daily`（无参数）显示帮助信息，不是报错 | 运行 `python daily.py` 无参数 |

## 实现方案对比（≥3 种）

| 方案 | 存储方式 | 优点 | 缺点 | 选定理由 |
|------|---------|------|------|---------|
| **A. Markdown 文件 + Python CLI** | `~/.daily_log.md` 纯文本 | 零依赖、编辑器可读、git 可追踪、需求方正好用终端 | 不支持并发写入（需求方单人使用无影响） | ✅ 选定 — 最简、最匹配需求方习惯 |
| **B. SQLite 数据库 + Python CLI** | `~/.daily.db` | 查询灵活、支持复杂检索 | 需要编辑器额外工具才能看、需求方不需要高级查询 | ❌ 过度设计，需求方明确说"越简单越好" |
| **C. JSON 文件 + Python CLI** | `~/.daily.json` | 结构化、易解析 | 编辑器不可读（JSON 阅读体验差）、git diff 不直观 | ❌ Markdown 的人类可读性好太多 |
| **D. Google Sheets / Airtable** | 云端 | 手机也能看 | 依赖网络、依赖第三方服务、不符合"本地文件"的约束 | ❌ 违反不依赖网络约束 |

**选定方案 A：** Markdown + Python CLI。理由：需求方每天在终端，不想看网页；Markdown 任何编辑器都能打开，git 版本控制友好；纯文本 100 年后也能读。
